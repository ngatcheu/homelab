---
- name: Créer le répertoire des manifestes RKE2
  ansible.builtin.file:
    state: directory
    path: "{{ rke2_data_path }}/server/manifests"
    owner: root
    group: root
    mode: 0700

- name: Copier les fichiers Traefik vers le premier serveur
  ansible.builtin.template:
    src: "templates/traefik-config.yml.j2"
    dest: "{{ rke2_data_path }}/server/manifests/rke2-traefik-config.yaml"
    owner: root
    group: root
    mode: 0664
